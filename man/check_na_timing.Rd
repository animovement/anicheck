% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_na_timing.R
\name{check_na_timing}
\alias{check_na_timing}
\alias{check_na_timing.default}
\alias{check_na_timing.aniframe}
\title{Check the Timing of Missing Values}
\usage{
check_na_timing(data, ...)

\method{check_na_timing}{default}(data, ...)

\method{check_na_timing}{aniframe}(data, variable = "x", ...)
}
\arguments{
\item{data}{An aniframe object.}

\item{...}{Additional arguments (currently unused).}

\item{variable}{Name(s) of the column(s) whose missingness to track. A frame
counts as missing when \emph{any} named column is \code{NA} there. Defaults to
\code{"x"}.}
}
\value{
A data frame of class \code{check_na_timing} with one row per missing run:
the aniframe's grouping columns (every \code{variables_what} and non-time
\code{variables_when} column), the run's \code{start} and \code{stop} time, and its
\code{length} in frames. Per-group totals (frame and missing counts, time
range), the checked variable(s), the time unit, and the typical time step
are stored as attributes for the \code{summary()}, \code{print()}, and plotting
methods. Use \code{\link[=summary]{summary()}} for a per-group overview.
}
\description{
Finds the runs of consecutive missing values (\code{NA}) in an aniframe and
returns them as a compact table — \strong{one row per gap}, not per frame. Where
the gaps fall (at the start, the end, scattered, or in long bursts) is often
more telling than how many there are; this check exposes that timing, and
\code{\link[=plot.check_na_timing]{plot.check_na_timing()}} draws it as a missingness strip.
}
\details{
Returning gaps rather than per-frame rows keeps the object (and any plot built
from it) tiny even for million-frame recordings: its size scales with the
number of gaps, not the length of the data.

This is the data-generating half of the check. The plotting method
(\code{\link[=plot.check_na_timing]{plot.check_na_timing()}}) lives in \pkg{anivis}, mirroring the
\pkg{performance} / \pkg{see} split in easystats: \verb{check_*()} computes a
classed object with \code{summary()} / \code{print()} methods, and a \verb{plot.*()} method
in the companion package draws it. (\verb{check_*()} functions are destined for the
\pkg{anicheck} package; they are kept here for now for convenience.)
}
\examples{
af <- aniframe::as_aniframe(data.frame(
  keypoint = rep(c("head", "tail"), each = 6),
  time = rep(1:6, 2),
  x = c(1, NA, NA, 4, 5, 6, 1, 2, 3, 4, 5, 6)
))
check_na_timing(af)
summary(check_na_timing(af))

}
\seealso{
\code{\link[=plot.check_na_timing]{plot.check_na_timing()}}, \code{\link[=summary.check_na_timing]{summary.check_na_timing()}}
}
